package BubMod.view.components {		import flash.display.Sprite;	import flash.text.*;		public class Table extends Sprite {				// Constants:		// Public Properties:		// Private Properties:		private var lastX:Number = 0;		private var tableWidth = 20;		private var tableBox:Sprite			// Initialization:		public function Table(table:String) { 					tableBox = new Sprite();			// drawing a green rectangle            tableBox.graphics.beginFill(0x33CC99); // white            tableBox.graphics.drawRect(0,0,300,100); // x, y, width, height            tableBox.graphics.endFill();             // drawing a white border            tableBox.graphics.lineStyle(8, 0xFFFFFF, 80);  // line thickness, line color (black), line alpha or opacity            tableBox.graphics.drawRect(0,0,300,100); // x, y, width, height						//Create a text format for the table nam			var attrFormat:TextFormat = new TextFormat();			attrFormat.size = 60;			attrFormat.align = TextFormatAlign.CENTER;			//Create a textfield for the table name			var textfield:TextField = new TextField();		    textfield.wordWrap = true;            textfield.text = table;			textfield.width = 300;			textfield.textColor = 0xFFFFFF;			textfield.setTextFormat(attrFormat);			//Add the table name and box sprites to the table sprite            this.addChild(tableBox);               this.addChild(textfield);		}				public function addAttribute(attributeName:String):void {			//Add 100 pixels to the last x position			lastX += 100;			var attributeBox:Sprite = new Sprite();			// drawing a green rectangle            attributeBox.graphics.beginFill(0x33CC99); // green            attributeBox.graphics.drawRect(0,lastX,300,100); // x, y, width, height            attributeBox.graphics.endFill();             // drawing a white border            attributeBox.graphics.lineStyle(4, 0xFFFFFF, 80);  // line thickness, line color (black), line alpha or opacity            attributeBox.graphics.drawRect(0,lastX,300,100); // x, y, width, height						//Create a text format for the attribute			var attrFormat:TextFormat = new TextFormat();			attrFormat.size = 60;			attrFormat.align = TextFormatAlign.CENTER;			//Create a new text field for the attribute			var textfield:TextField = new TextField();		    textfield.wordWrap = true;            textfield.text = attributeName;			textfield.width = 300;			textfield.textColor = 0xFFFFFF;			textfield.setTextFormat(attrFormat);			textfield.y = lastX;			//Add the attribute name and box to the table sprite            this.addChild(attributeBox);               this.addChild(textfield);		}			// Public Methods:		// Protected Methods:	}	}